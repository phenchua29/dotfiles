###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = ALT, M, killactive,
bind = $mainMod, E, exec, $fileManager
bind = ALT, F, fullscreen 
bind = $mainMod, V, togglefloating,
# bind = $mainMod, P, exit,
bind = $mainMod, P, pseudo, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, L, movefocus, l
bind = $mainMod, H, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = ALT, Q, workspace, 1
bind = ALT, W, workspace, 2
bind = ALT, E, workspace, 3
bind = ALT, A, workspace, 4
bind = ALT, S, workspace, 5
bind = ALT, D, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9] (silent = stay on current workspace)
bind = ALT SHIFT, Q, movetoworkspacesilent, 1
bind = ALT SHIFT, W, movetoworkspacesilent, 2
bind = ALT SHIFT, E, movetoworkspacesilent, 3
bind = ALT SHIFT, A, movetoworkspacesilent, 4
bind = ALT SHIFT, S, movetoworkspacesilent, 5
bind = ALT SHIFT, D, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-
bindel = ALT, Prior, exec, brightnessctl s 30%
bindel = ALT, Next, exec, brightnessctl s 0%

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Custom
$launchPrefix = uwsm app --

bind = ALT, SPACE, exec, $launchPrefix rofi -show drun 
bind = ALT, P, exec, $launchPrefix vicinae toggle
bind = ALT, T, exec, $launchPrefix rofi -show window 
bind = ALT SHIFT, R, exec, killall waybar && $launchPrefix waybar
bind = $mainMod SHIFT, S, exec, $launchPrefix grimblast copy area

bind = ALT SHIFT, L, exec, kitty -e $HOME/fpt/codes/caro-universe-v2/run.sh
# bind = SUPER, B, exec, killall -SIGUSR1 waybar
bind = SUPER, B, exec, wayle panel toggle
